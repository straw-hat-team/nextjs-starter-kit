import type { PropsWithChildren } from 'react';

export function {{ pascalCase name }}(props: PropsWithChildren<{}>) {
  return (
    <div>
      <h1>{{ titleCase name }}</h1>
      {props.children}
    </div>
  );
};
